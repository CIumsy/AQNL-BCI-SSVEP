# Python
__pycache__/
*.py[cod]
.venv/
venv/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Written on the board by each calibration run; valid for 30 minutes on
# the machine that produced it, so it is never shared.
fbcca_thresholds.json

# Recordings written by calibration runs on the board.
sessions/*.npz
sessions/plots/
